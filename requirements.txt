# ============================================================
#  local_os — project dependencies
#  Python >= 3.10 required
#
#  Install:   pip install -r requirements.txt
#  Dev+test:  pip install -r requirements.txt -r requirements-dev.txt
#
#  Sections
#  --------
#  [core]     always required
#  [ui]       terminal rendering
#  [crypto]   vault / crypto module
#  [network]  network / firewall modules
#  [system]   system_info / process_manager
#  [storage]  sqlite_shell / scheduler / vault persistence
#  [git]      git_tools module
#  [docker]   docker_manager module
#  [logs]     log_analyzer module
#  [geo]      optional geo-ip (firewall geoip lookup)
#  [api]      optional REST API server (Priority 5)
#  [notify]   optional notifier module (Priority 5)
# ============================================================

# ── [core] ───────────────────────────────────────────────────
# environment & config
python-dotenv>=1.0.0

# CLI argument parsing (stdlib argparse is used, but click is
# available for plugin authors)
click>=8.1.0

# date/time utilities
python-dateutil>=2.8.2

# ── [ui] ─────────────────────────────────────────────────────
# rich terminal UI — tables, panels, live dashboards, progress bars
rich>=13.7.0

# table formatting (fallback / export)
tabulate>=0.9.0

# ── [crypto] ─────────────────────────────────────────────────
# symmetric encryption (Fernet, AES)
cryptography>=42.0.0

# password hashing: PBKDF2, bcrypt, scrypt
passlib[bcrypt]>=1.7.4

# TOTP / HOTP (vault 2FA)
pyotp>=2.9.0

# RSA key generation & signing
# (already included in cryptography ↑)

# ── [network] ────────────────────────────────────────────────
# HTTP requests (network inspect, geo-ip fallback)
requests>=2.31.0

# async HTTP (optional, faster scans)
httpx>=0.27.0

# DNS resolution / whois
dnspython>=2.6.0
python-whois>=0.9.4

# port scanner helpers (low-level socket is stdlib;
# scapy is optional — install separately if raw-packet features needed)

# ── [system] ─────────────────────────────────────────────────
# cross-platform OS/CPU/RAM/Disk/Battery/GPU info
psutil>=5.9.8

# GPU info (optional — graceful degradation if not installed)
# gputil>=1.4.0        # uncomment if NVIDIA GPU support needed
# pynvml>=11.5.0       # alternative NVML binding

# ── [storage] ────────────────────────────────────────────────
# SQLite is stdlib; these add ORM / migration helpers for plugins
# (core modules use raw sqlite3 — no extra deps)
# sqlalchemy>=2.0.0    # uncomment if plugins need ORM

# ── [git] ────────────────────────────────────────────────────
# git operations (git_tools.py)
gitpython>=3.1.43

# ── [docker] ─────────────────────────────────────────────────
# Docker SDK for Python (docker_manager.py)
docker>=7.1.0

# ── [logs] ───────────────────────────────────────────────────
# fast log file tailing / watching
watchdog>=4.0.0

# structured log parsing helpers
# (log_analyzer uses stdlib re + csv; watchdog is the only extra dep)

# ── [scheduler] ──────────────────────────────────────────────
# cron expression parsing (scheduler.py)
croniter>=2.0.5

# ── [geo] — optional, install manually if needed ─────────────
# geoip2>=4.8.0          # MaxMind GeoLite2 reader
# maxminddb>=2.6.0       # low-level mmdb reader (dep of geoip2)

# ── [api] — Priority 5, uncomment when building api_server ──
# fastapi>=0.111.0
# uvicorn[standard]>=0.30.0
# pydantic>=2.7.0

# ── [notify] — Priority 5, uncomment when building notifier ─
# python-telegram-bot>=21.0
# slack-sdk>=3.27.0
# plyer>=2.1.0           # desktop notifications

# ── [dev / test] — put in requirements-dev.txt ───────────────
# pytest>=8.2.0
# pytest-cov>=5.0.0
# pytest-asyncio>=0.23.0
# mypy>=1.10.0
# ruff>=0.4.0
# black>=24.0.0